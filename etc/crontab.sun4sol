# The nightly sun4sol build runs last, it creates the update ftp files and
# also checks disk bookings.
00 23 * * 0-3,5,6 . $HOME/.profile ; cd $MIR && gmake MIRATNF=1 allsys updates bookings 2>&1 | tee $MIRARCH/gmake.log
#
# In addition to the usual nightly tasks the weekly sun4sol build also
# regenerates the ftp distribution kits including binary kits for all of the
# architectures that we build locally.
00 23 * * 4 . $HOME/.profile ; cd $MIR && gmake MIRATNF=1 dist updates bookings 2>&1 | tee $MIRARCH/gmake.log
#
# $Id$
